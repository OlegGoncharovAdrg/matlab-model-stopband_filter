function [ out ] = expand_arr1 (arr, Ns)
%Ёта функция изменяет длину массива,
%  "растягивая" данные с использованием экстраполяции
%  нулевого порядка
%ѕараметры:
%  arr[1, N] - массив 
%  Ns        - требуемая длинна массива
N=size(arr, 2);

K=N/Ns;					%«апись ѕ—ѕ в массив требуемого размера
out=zeros(1,Ns);
i=1:Ns;
j=fix(K*(i-1))+1;
%for i=1:Ns
%    out(1,i)=arr(1,fix(K*(i-1))+1);
%end    
out(1,i) = arr(1, j);



